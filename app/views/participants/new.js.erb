$('#ajax-modal').html('<%= escape_javascript(render :partial => 'participants/new', :locals => {:watched => @watched, :users => @users}) %>');
showModal('ajax-modal', '400px');
$('#ajax-modal').addClass('new-watcher');
